image: ubuntu:18.04

before_script:
  - apt-get update -qy
  - apt-get install -y lftp

build:
  script:
    #- lftp -e "open ftp://waws-prod-dm1-145.ftp.azurewebsites.windows.net; user $FTP_USERNAME $FTP_PASSWORD; mirror -X .* -X .*/ --reverse --verbose --delete ./ ./site/wwwroot/one; bye"
    - lftp -c "set ftp:ssl-allow no; open -u $FTP_USERNAME,$FTP_PASSWORD ftp://waws-prod-dm1-145.ftp.azurewebsites.windows.net; mirror -v ./ /site/wwwroot/one --reverse --ignore-time --parallel=10 --exclude-glob .git* --exclude .git/"  
  environment:
    name: production
  only:
    - master